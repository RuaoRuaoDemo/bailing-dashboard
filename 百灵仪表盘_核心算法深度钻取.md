# 百灵仪表盘 核心算法深度钻取

> 分析对象：`C:\Users\RuaoWork\Desktop\fuke-bailing-dashboard\index.html` + `core/formulas.js`
> 分析基准：**真实源码逐行核对**（行号均来自实际文件）
> 配套文档：《百灵仪表盘_代码分析报告.md》《百灵仪表盘_交互与耦合分析.md》

---

## 0. 钻取结论速览

| 链路 | 真实行为（钻取确认） | 与旧认知/注释的差异 |
|------|---------------------|-------------------|
| `calcRow` | 全局 `calcRow`(L2520) **委托回** `BailingFormulas.calcRow`(formulas.js L99)；单一事实源在 formulas.js，全局内联 fallback 为死代码 | 不存在"两份并行实现"，但存在一份**永不执行的陈旧 fallback**，有误导性 |
| 利用率 Excel 值 | `precomputeUtilRateFinal`(L5206) **无条件用公式重算值**，`_utilRateFinal` 总是被赋值 → Excel 底表 `_utilRate` 实际**被完全忽略** | 注释(L5263)声称"与计算值差异>1%以计算值为准"，但代码**从不比较**，直接丢弃 Excel 值 |
| 11h 封顶修正 | `tongH_corrected = max(0, 11 - (tmH+meaH))`；当计量+计时已 >11h 时通航小时被**清零** | 激进修正，长工时员工利用率会被压到极低 |
| 白名单 | `App.scheduleWhitelist` **硬编码约 50 人**(L1517)，`whitelistSet` 仅加载时构建一次(L1535)；`syncGoogleSchedule`/`_parseGSheetAuto` **不更新**白名单，反而用白名单**过滤排班行**(L10376) | 白名单是**静态**的；新人若不在硬编码表，数据在 `calcAll`(L2673)被**静默裁剪**——根因确认 |
| 上传 ingest | `processAutoFile`(L10566) 按文件名关键词 `detectFileType` 路由；Excel 经 `normalizeSheetRows` 做表头别名纠正 | — |

---

## 1. `calcRow` 计算内核（formulas.js L99）

### 1.1 量纲（关键：审核类与计时类基准不同）
```
审核类：1 有效工时 = 1000 标准条 = eff 条审核量
  ∴ tongStd    = tong    / eff × 1000
  ∴ nontongStd = nontong / eff × 1000
  ∴ meterStd   = tongStd + nontongStd
计时类：1 小时 = 125 标准条
  ∴ timingStd  = minutes / 60 × 125
  ∴ totalStd   = meterStd + timingStd
```
⚠️ **审核类 1h≡1000 标准条，计时类 1h≡125 标准条**——两者量纲不一致，混合计 totalStd 时直接相加，物理含义是"审核当量 + 计时当量"，并非同一单位的简单求和。全天 8h 理论审核 ~1000 条，但含计时任务时 totalStd 会偏离 1000（正常）。

### 1.2 `_origStd` 信任 Excel 的两条分支
```js
if (origStd > 0) {
  const calcTotal = meterStd + timingStd;
  if (calcTotal > 0 && |origStd - calcTotal| / calcTotal > 0.05) {
    // 分支A：差异>5% → 按比值整体缩放各分项（保持分项结构，仅拉伸总量）
    const ratio = origStd / calcTotal;
    finalTongStd = tongStd * ratio; ... finalTotal = origStd;
  } else if (calcTotal === 0 && (tong>0||nontong>0||minutes>0)) {
    // 分支B：算出来为0但 Excel 有值 → 按 tong/nontong/minutes 权重拆分 origStd
    const sum = tong + nontong + minutes;
    finalTongStd = origStd * tong / sum; ...
  }
}
```
- **分支A**：当公式算出的合计与 Excel 给定总标准条相差 >5%，**信任 Excel**，按比值缩放全部分项。风险：若某一分项（如 `eff` 解析错误导致 `tongStd=0`）被缩放，可能放大错误。
- **分支B**：当公式算得 0 但 Excel 有值（典型 `eff` 缺失导致审核类全 0），用 `tong/nontong/minutes` 的**原始量权重**拆分 `origStd`。注意权重用原始条数/分钟数，不是标准条，近似合理。

### 1.3 全局 `calcRow`（index.html L2520）是薄包装，非独立实现
```js
function calcRow(r) {
  if (typeof BailingFormulas !== 'undefined' && BailingFormulas.calcRow)
    return BailingFormulas.calcRow(r);   // ← 实际走这里
  // --- Fallback：陈旧实现，逻辑更简（缺分支B，且 origStd=0 时直接 finalMeter=origStd） ---
  ...
}
```
- `BailingFormulas` 通过 `<script src="core/formulas.js">` 常驻加载，故 `calcAll` 调用 `calcRow(r)` 永远走 formulas.js 版本。
- **结论**：不存在"两份逻辑并行"的隐患，但全局内联 fallback（L2525–2546）是**死代码且逻辑落后**（未实现分支B的加权拆分），一旦 formulas.js 加载失败会退回错误算法。建议删除该 fallback 并改 `calcAll` 直接 `BailingFormulas.calcRow(r)`。

### 1.4 回归自测
`BailingFormulas.selfTest()`(L307) 覆盖基础换算、calcRow 各分支、利用率分级、异常判定，启动时自动运行（L11922）。**注意**：selfTest 的 calcRow 用例未覆盖"分支B（eff=0 但 origStd 有值）"的加权拆分正确性，回归覆盖存在盲区。

---

## 2. 利用率计算链（正确性关键）

### 2.1 标准公式（formulas.js L179 `calcUtilRateByHours`）
```
利用率% = ((计量h + 计时h) × 0.8125 + 通航审核h) / 出勤h × 100
```
非任务**不计入分子**（非任务不创造有效工时）。`NONTASK_EFFECTIVE_WEIGHT = 0.8125` 即 0.65 有效工时系数经 125 归一（注释 L18–19）。

### 2.2 `precomputeUtilRateFinal`（L5206）—— 全局预计算
对每个 `utilRow`：
1. **11h 封顶修正**（L5217–5224）：
   ```js
   const totalH = tongH + tmH + meaH;
   if (totalH > 11) {
     baseAnomaly = true;
     tongH_corrected = Math.max(0, 11 - (tmH + meaH));  // ← 当 tmH+meaH≥11，通航直接清零
   }
   ```
   ⚠️ **边界缺陷**：若某员工"计量+计时"已达 11h（如长班次/加班），`tongH_corrected=0`，其通航审核时长在利用率中被完全抹除，利用率被严重低估。修正逻辑只压通航、不压计量/计时，存在系统性偏差。
2. **展示率**：`displayRate = ((meaH+tmH)*0.8125 + tongH_corrected) / attendH * 100`。
3. **覆盖优先级**（L5242–5251）：`utilRateOverrides[mk1/mk2]` > `ur._overrideRate` > `displayRate`。**Excel `_utilRate` 不参与 final**。

### 2.3 `getUtilRateFinal`（L5188）取值链
```
_overrideRate > _utilRateFinal > _utilRateDisplay > _utilRate(Excel)
```
由于 `precomputeUtilRateFinal` 必然写 `_utilRateFinal`，故 `getUtilRateFinal` 实际恒等于 `finalRate`（覆盖值或公式重算值），**Excel `_utilRate` 永远到不了第 4 级**。

### 2.4 发现：注释与行为不符
- `calcUtilStats`(L5263) 注释："Excel 底表 `_utilRate` … 若与计算值差异超过 1%，以计算值为准。"
- **实际代码未做任何 1% 比较**，而是无条件以公式重算值为准（除非手动覆盖）。
- 影响：若 Excel 底表的 `_utilRate` 来自更权威的核算系统（如注释提到的"杜磊 4-30 的 1.1%"异常值），该值被完全忽略，系统用自身公式重算——这可能是**有意设计**（避免脏数据），也可能**丢失可信源**，需在文档中明确声明取舍。

### 2.5 一致性确认
`openDetail`(L6972) 与 `renderPersonSummary`(L11248) 均使用 `getUtilRateFinal`/`_utilRateFinal`，**利用率口径全局一致**；前期报告提出的"openDetail 与 calcAll 口径漂移"**仅限于标准条合计**（openDetail 用正则防双算重算），**不波及利用率**——此点已澄清。

---

## 3. 白名单 / 排班数据管道（破坏性裁剪根因）

### 3.1 硬编码白名单（L1517–1539）
```js
App.scheduleWhitelist = { 'xiaoranchen':'陈晓冉', 'kxhwang':'王敬凯', ... 约50人 }; // 静态对象
App.whitelistSet = new Set();
for (const en in App.scheduleWhitelist) {
  App.whitelistSet.add(en);            // 英文名
  App.whitelistSet.add(App.scheduleWhitelist[en]); // 中文名
}
```
- 白名单是**写在源码里的硬编码映射**（英文名→中文名），仅在脚本加载时构建一次。
- 含 `ly-*` 前缀 ID（如 `'ly-zhangzhanshuai':'张占帅'`）与个别特殊键（如 `'A10':'李亚帆'`）。

### 3.2 `calcAll` 用白名单裁剪（L2673–2741，已确认）
对 `dailyData/utilRows/rawRows/nontaskRows` **原地 `.filter()`**，白名单外（且 org 不匹配）的行被**永久删除**。

### 3.3 排班同步**不扩展**白名单（根因确认）
`syncGoogleSchedule`(L10214) → `_fetchGSheetJSONP`(L10277) → `_parseGSheetAuto`(L10316)：
- `_parseGSheetAuto` **不写** `scheduleWhitelist`/`whitelistSet`，只写 `App.scheduleMap` 与 `enToCnMap`/`empEnMap`(L10392)。
- 它反用白名单做**入参过滤**（L10357–10376）：
  ```js
  const knownEmps = new Set();
  if(App.whitelistSet) for(const v of App.whitelistSet) knownEmps.add(v); // 白名单是已知员工来源之一
  ... // 还合并 dailyData/rawRows/utilRows/enToCnMap
  if(knownEmps.size > 0 && !knownEmps.has(empEn) && !knownEmps.has(empCn)) continue; // 白名单外的排班行被跳过
  ```
- 因此：**Google  Sheet 里的新人，若不在硬编码 `scheduleWhitelist` 中，其排班行被跳过、且不入白名单；其工作量数据在 `calcAll` 又被裁剪**。新人 = 排班无、数据无——全盘消失，且无任何告警。

### 3.4 `knownEmps` 造成的"半同步"不一致
`knownEmps` 含 `App.enToCnMap`(L10363)，而 `enToCnMap` 在 `calcAll`(L2558) 由 `rawRows` 重建。于是：
- 若新人仅在**工作量 CSV** 出现（进 enToCnMap）→ `_parseGSheetAuto` 会为其建排班（`scheduleMap` 有此人）。
- 但 `calcAll` 随后用 `whitelistSet` 裁剪 `dailyData`，此人**不在白名单**→ 被剔除。
- 结果：**班表矩阵里有这个人，主看板/日汇总/异常里没有**——跨视图数据不一致（前期报告已预警，此处给出代码级证据）。

### 3.5 结论
白名单静态化是系统最大数据完整性风险。修复方向：
- **动态白名单**：以"排班表 + 工作量表 + 利用率表的并集员工"为白名单来源，或提供 UI 编辑白名单；
- **裁剪前备份**：`calcAll` 白名单过滤前先 `App._rawRowsBackup = cloneDeep([...rawRows,...nontaskRows])`，供"查看被剔除人员"；
- **剔除告警**：白名单过滤后若 `beforeCount - afterCount` 较大，弹出"N 人被白名单排除"提示。

---

## 4. 上传 ingest 管道（processAutoFile，L10566）

### 4.1 类型探测
`detectFileType`(L10555) 按文件名小写关键词：
`排班/班表/schedule/班次` → schedule；`非任务/nontask` → nontask；`工作量/workload` → workload；`工时利用率/util` → util；否则 unknown。

### 4.2 CSV 分支
`decodeCsvBuffer`(L10505) 做 **UTF-8/GBK 自动探测**（按 `�` 乱码计数 fallback 到 gb18030）；`parseCSVText`(L10515) 跳过模板说明行、定位真实表头。

### 4.3 Excel 分支（多 Sheet 路由，L10599–10631）
按 Sheet 名关键词顺序判定：
1. 排班（`排班`/`班`）；
2. 利用率（`工时利用率`/`util`/`工时`/`利用率`）；
3. 工作量（`工作量` 或单 Sheet 且未设 wl）；
4. 非任务（`非任务`）；
5. 兜底：首 Sheet→工作量、次 Sheet→非任务。
所有识别路径都经 `normalizeSheetRows(sheet, WORKLOAD_RULES/NONTASK_RULES, ...)` 做**表头别名→规范字段映射**（`_emp/_eff/_tong/_nontong/_minutes/_origStd/_attendH/_utilRate` 等），保证不同导出格式统一。

### 4.4 覆盖模式
`bindUploadEvents`(L10688) 上传前**清空全部状态 + 删 localStorage**，再逐文件 `processAutoFile`，最后 `calcAll();renderAll();saveToLocalStorage();` 并 `await syncGoogleSchedule()`(L10717)——即上传后强制用线上 Google Sheet 覆盖排班。
⚠️ 若 Google Sheet 不可用（网络/权限），排班同步静默跳过（L10717 catch），但工作量已加载，此时白名单裁剪仍生效。

---

## 5. 可执行修复建议（按优先级）

| 优先级 | 问题 | 建议 |
|--------|------|------|
| P0 | 白名单硬编码导致新人数据静默丢失 | 白名单改为动态（排班+工作量+利用率并集），或加 UI 编辑 + 剔除告警 |
| P1 | 11h 封顶把长工时员工通航清零 | 改封顶逻辑：按各分量同比例压缩，而非只压通航；或仅对"明显异常"行标记不修正 |
| P1 | Excel `_utilRate` 被无条件忽略（与注释不符） | 在 `precomputeUtilRateFinal` 实现注释声称的 1% 比较，或更正注释明确"以公式为准" |
| P2 | 全局 `calcRow` 内联 fallback 死代码且落后 | 删除 L2525–2546 fallback，`calcAll` 直接调用 `BailingFormulas.calcRow` |
| P2 | selfTest 未覆盖 `_origStd` 分支B | 增加 eff=0/origStd 有值 的加权拆分断言 |
| P3 | 裁剪前无备份 | 裁剪前深拷贝原始行供审计 |

---

*本钻取所有结论均来自对 `index.html` 与 `core/formulas.js` 的逐行阅读；行号指向真实位置，可直接跳转核验。与《代码分析报告》《交互与耦合分析》配套使用。*
