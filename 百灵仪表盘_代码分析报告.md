# 百灵仪表盘（Bailing Dashboard）代码分析报告

> 分析对象：`C:\Users\RuaoWork\Desktop\fuke-bailing-dashboard\index.html` 及其 `core/` 模块
> 分析基准：**真实源码逐行核对**（非凭记忆推断）
> 文件版本标识：`<!-- Bailing Dashboard V9.0 - 20260521 - 上传模式+下载离线版 -->`（L1）

---

## 0. 重要修订说明（本版相对错误旧稿的校正）

旧版分析存在多处与真实源码不符之处，本版已全部据实修正：

| 项目 | 旧版（错误） | 真实源码 |
|------|-------------|---------|
| 文件形态 | 单文件 13128 行全内联 | 模块化结构，`index.html` 13945 行，经 `<script src="core/*.js">` 与 `<script src="data/persistence.js">` 加载外部模块 |
| 版本号 | V8.x | 文件级版本 **V9.0**（源码内散见的 `V8.xx` 是历史补丁注释标签，非文件版本） |
| 白名单变量 | `SCHEDULE_EMP_WHITELIST` | **`App.whitelistSet`**（由 `App.scheduleWhitelist` 初始化，L1536–1538） |
| `calcAll` 行号 | @2384 | **@2551** |
| `openDetail` 行号 | @6739 | **@6914** |
| `showView` 行号 | @10883 | **@11690** |
| `applyFilters` 行号 | 未给出 | **@3682** |
| `renderTable` 行号 | 未给出 | **@3830** |
| `renderAll` 行号 | 未给出 | **@11725** |
| `MutationObserver` | 不存在 | **存在**，位于 `mountRangePickers` 内 L11981 |
| `index.html` 与 `bailing-dashboard.html` | 内容不同 | 两文件**完全相同（均为 13945 行）**，`bailing-dashboard.html` 为冗余副本 |

---

## 1. 总体架构

### 1.1 技术栈
- **纯前端零构建**：无打包器、无 npm 依赖，直接在浏览器打开 `index.html` 即可运行。
- 运行时依赖三个 CDN：Tailwind（`cdn.tailwindcss.com`）、Chart.js 4.4.0、SheetJS（`xlsx.full.min.js`）。
- 离线能力：`downloadBoardAsHTML()`（L10999）会把所有 `core/*.js` 与 `styles.css` 内联进一个独立 HTML，并注入当前数据快照（`window._BAILING_SNAPSHOT`），生成可脱离服务器分发的「离线版」。

### 1.2 文件职责

| 文件 | 行数（约） | 职责 |
|------|-----------|------|
| `index.html` | 13945 | 主应用：DOM 结构 + 全部业务逻辑（初始化、计算、渲染、交互、事件绑定） |
| `core/store.js` | 111 | 全局状态单例 `App` 的定义、重置、员工映射表重建 |
| `core/utils.js` | 73 | `$`/`$$`/`escHtml`/`escAttr`/`excelDateToString`/`norm`/`toNumber`/`cloneDeep`/`debounce` |
| `core/formulas.js` | 396 | 公式常量与计算：标准条换算、利用率、异常分级、自测 `selfTest()` |
| `core/multi-select.js` | 208 | 多选下拉组件 `BailingMultiSelect` |
| `core/range-picker.js` | 441 | 日期范围选择器 `BailingRangePicker`（含月历网格、快捷键） |
| `data/persistence.js` | 84 | `BailingPersistence`：localStorage 读写（仅存原始行 + 用户选择） |
| `styles.css` | 689 | 全部视觉样式（设计令牌、表格、状态色、班表矩阵、Modal 等） |
| `views/` | 空 | 预留目录，当前未使用 |
| `百灵数据/` | — | 空壳 xlsx + 说明，仅作模板占位 |

### 1.3 设计模式
**「全局状态单例 + 命令式整片重绘」**：
- `App`（`window.App`，由 `core/store.js` 暴露为 `BailingStore.App`）是**唯一事实源**。
- 数据变化后不追求局部更新，而是经 `calcAll()` 重新聚合 → `applyFilters()` 过滤 → 各 `render*()` 整片重写 `innerHTML`。
- 三大重绘枢纽：`calcAll()` / `renderAll()` / `applyFilters()`（详见《交互与耦合分析》）。

---

## 2. 全局状态 `App`（core/store.js）

`App` 持有：
- **原始数据**：`rawRows`（工作量原始行）、`nontaskRows`（非任务工时行）、`utilRows`（利用率底表行）。
- **聚合数据**：`dailyData`（按「员工×日期」合并后的日维度行）、`filteredData`（`applyFilters` 后的当前视图数据）。
- **排班**：`scheduleMap`、`scheduleMatrix`。
- **员工映射表（4 张 Map）**：`empUinMap` / `uinToEmpMap` / `empEnMap` / `enToCnMap`，用于在「中文名 / 英文名 / 工号」三种标识间互转。这是整个系统能正确合并同名不同人的关键。
- **白名单**：`whitelistSet`（Set）、`scheduleWhitelist`（对象）。
- **用户态集合**：`ignoredKeys` / `manualDeletedKeys` / `selectedRowKeys`（均为 Set）。
- **视图态**：`currentPage=1` / `pageSize=20` / `sortField='abnormal'` / `sortDirection='desc'`。
- **阈值**：`thresholdLow=995` / `thresholdHigh=1005`（标准条日合计合理区间）。
- **组织周期配置**：`groupRules` / `groupExclude` / `orgPeriodConfig`。
- **利用率覆盖**：`utilRateOverrides`。
- **图表引用**：`App.*Chart`（含 `detailDoughnut`、`detailPeriodChart`、`auditChartExpand` 等）；重绘前通过 `safeDestroyChart(key)`（L9175）销毁旧实例以防泄漏。
- `debugMode` 开关。

---

## 3. 计算层：`calcAll()`（L2551）

这是数据从「原始行」到「日维度聚合」的总入口，顺序如下：

1. **重建员工映射**（L2553）：优先委托 `BailingStore.rebuildEmpMaps()`，否则用 `rawRows`/`nontaskRows` 兜底重建 4 张 Map。
2. **合并工作量 → dayMap**（L2582–2596）：以 `rawRows` 为名单基准，按 `makeKey(empEn, emp, uin, date)` 累加 `calcRow(r)` 的结果（通航/非通航/计时/计量标准条）。
3. **合并非任务 → 同一 dayMap**（L2609–2663）：
   - V9.0：合并前先做 **org 预过滤** `ORG_KEYWORDS_PRE = ['H1组','嘉兰项目','嘉兰']`（L2601），排除非目标组织。
   - 对每条非任务行尝试多种候选 key 匹配（英文名 / 中文名 / 工号 + 映射反查）。
   - **V9.1 规则**：若非任务行有明确英文名/工号，与匹配到的 dayMap 行必须一致，否则**拒绝合并**（防同名不同人污染）。
   - 若该员工当天**只有非任务、没有工作量**，则**新建 dayMap 条目**（V9.1，L2637），不再丢弃。
4. **`App.dailyData = Array.from(dayMap.values())`**（L2668）。
5. **严格白名单过滤（V8.92 / V9.2）**（L2673–2741）：这是本系统的「权威裁剪」环节，**对四个数组原地 `.filter()` 永久改写**，丢弃白名单之外（及 org 不匹配）的原始行：
   - `dailyData` → `isWhitelistedStrict`（白名单 + 同名 org 双重校验）。
   - `utilRows` → 白名单基础过滤 + **交叉校验**（该人必须已在 `dailyData` 中出现过，否则视为同名不同人排除）。
   - `rawRows` / `nontaskRows` → 同样按白名单 + org 过滤。
   - ⚠️ 副作用：白名单外的原始行被**物理销毁**，无法在「查看原始数据」类功能中恢复。
6. **`applyManualRows()`**（L2743）：套用用户手动录入/修正行。
7. **排序**（L2744）：按日期 + 英文名。
8. **`precomputeUtilRateFinal()`**（L2747，定义于 L5206）：预计算每条 utilRow 的 canonical 利用率（含 11h 封顶修正 + 手动覆盖），供下游视图统一读取，避免直读脏数据。

`calcAll` 本身**不直接渲染**，它只改写 `App.dailyData` 等状态；渲染由调用方（初始化块或 `renderAll`）负责。

---

## 4. 公式与阈值层（core/formulas.js）

关键常量（`BailingFormulas.CONSTANTS`）：
- `STD_PER_HOUR = 125`（标准条/小时；非任务 `nts = nr._minutes/60*125`）。
- `NONTASK_EFFECTIVE_WEIGHT = 0.8125`（非任务有效权重）。
- `UTIL_PASS = 81.25%` / `UTIL_WARN = 70%` / `UTIL_OVER = 100%` / `UTIL_OVER_HIGH = 105%` / `UTIL_OVER_MEDIUM = 102%`。
- `WORKLOAD_LOW_DEFAULT ≈ 989.58` / `WORKLOAD_HIGH_DEFAULT ≈ 1010.42`（阈值 995/1005 的容差派生）。
- `NONTASK_SPIKE_RATIO = 0.3`、`DEFAULT_ATTEND_HOURS = 8`、`UTIL_HOUR_CAP = 11`。
- 利用率公式：`calcUtilRateByHours = ((measure + timing) * 0.8125 + tong) / attend * 100`。

`calcRow`（审核 ×1000、计时 ×125）：当 `_origStd` 与公式结果差异 > 5% 时，**信任 Excel 原始值**（避免双重换算失真）。
`BailingFormulas.selfTest()` 启动时自动运行回归（L11922）。

---

## 5. 渲染层

### 5.1 `renderAll()`（L11725）
全量刷新入口，按顺序：`renderMultiChecks`（员工/组织多选）→ 重建 `filterGroup` 选项 → `autoSetFilterDateRange()`（按分组智能定周期）→ 回填阈值/分页/排序控件 → **`renderTable(); renderSummary(); renderTrend(); checkAlerts(); updateFileCards(); renderAuditView();`**（L11756）。

### 5.2 `applyFilters()`（L3682）
当前视图数据的过滤器，构建 `App.filteredData`：
- 读取多选（员工 `filterNameMulti` / 组织 `filterOrgMulti`）、状态、日期、分组。
- 分组筛选预处理：构建 `grpEmpSet`（允许名单）与 `grpExcludeSet`（排除集），含**模糊匹配**与**遍历 `dailyData` 兜底**（V8.33，确保排除成员的所有标识都被剔除）。
- 最终 `App.filteredData = App.dailyData.filter(...)`，并 `_injectAttendNoDataRows()` 注入「出勤无量级」虚拟行。
- 末尾**焊死渲染**：`App.currentPage=1; renderTable(); renderSummary(); renderTrend(); checkAlerts();`（L3814）。

### 5.3 `renderTable()`（L3830）
- 清空 `tbody`，按 `sortDailyData` + 分页切片生成行。
- 所有动态值经 `escAttr`/`escHtml` 转义（L3842–3856、L3865–3868）。
- 行内 `onclick` 调用全局函数：`toggleIgnoreRow('${escKey}')` / `editDailyRowByKey('${escKey}')`（L3872–3873）。
- `tr.onclick` 委托 `openDetail`（排除 input/button/`.whitespace-nowrap`）。

### 5.4 明细视图 `openDetail()`（L6914）
- 若 `rows` 为空，从 `App.rawRows` 现查（L6917）。
- 记录来源 `App._detailSource`（audit / dashboard / alert），据此设置返回按钮文案（L6935）。
- **独立重算当日汇总**：从 `App.rawRows`/`App.nontaskRows` 再跑一遍 `calcRow` 累加，并用正则 `/培训|考试|开会|值班|还休|其他审核任务|团建|会议/`（L6943）比对非任务表，**防双算**。
- 查 `utilRows` 取得利用率，渲染 KPI / 历史均值 / 7 日趋势。
- 圆盘图：`App.detailDoughnut.destroy()`（L7114）后重建。

### 5.5 其他视图渲染
- `renderPersonSummary()`（L11175）：人维度周期汇总，按班表顺序排员工，利用率取 `_utilRateFinal`。
- `renderManageBoard()`（L11378）：Google Sheet 风格矩阵（嘉兰组 1日~月末 / H1组 21日~20日）。
- `renderAuditView()`（L9003）：审核视图，填充员工/类目/分组的下拉与筛选。
- `renderAnomalyView()`（L8639）：异常分析，数据来自 `AppEnhancedAlerts.all`。
- `renderScheduleView()`（L9684）：排班矩阵。

---

## 6. 持久化与初始化

### 6.1 持久化（`data/persistence.js` + L11761）
- 键名：`bailing_data` / `bailing_schedule`。
- `saveToLocalStorage()`（L11761）写入 `bailing_data`；超 4.5MB 时降级（丢 `rawRows`）。
- `loadFromLocalStorage()` 仅恢复原始行 + 用户选择，聚合在内存中重算。

### 6.2 初始化序列（L11822–11944）
1. 定义 `$`（L11823）。
2. `bindSchedImport()` / `bindUploadEvents()`（L11826–11827）。
3. `showView('dashboard')`（L11831）强制只显示主看板。
4. nav-btn 绑定：`audit`/`schedule`/`manage` → `showView(v)`（L11833）。
5. `loadFromLocalStorage()`（L11835）→ 诊断日志（L11837）。
6. 若有数据：`calcAll(); renderAll();`（L11848）+ `setTimeout` 150ms 二次渲染（V8.23 非任务聚合时序修复，L11853）。
7. `window.bailingDiagnose`（L11864）供控制台诊断数据流。
8. 再次确保非主看板隐藏（L11916–11918）。
9. `BailingFormulas.selfTest()` 自动运行（L11922）。
10. `handleHashRouting()`（L11931）：hash → `showView`，监听 `hashchange`，支持从独立页跳转。
11. `mountRangePickers()`（L11947）：挂载所有日期范围对，**含 `MutationObserver`（L11981）**动态挂载异常筛选 Modal 内的日期选择器。

---

## 7. 已知风险点（代码层面）

1. **白名单破坏性裁剪**：`calcAll` 对 `rawRows`/`nontaskRows`/`utilRows` 原地 `.filter()`，白名单外原始数据永久丢失，无法审计或回看。
2. **`openDetail` 与 `calcAll` 口径漂移**：`openDetail` 独立重算当日汇总且用正则防双算，而 `calcAll` 在主表合并时已对非任务做了合并；两套口径若对「培训/开会」等边界任务的处理不一致，明细页合计可能与主表不一致。
3. **计算与渲染焊死**：`applyFilters`（L3814）与 `renderAll`（L11756）末尾都强制触发整片重绘，任何过滤/排序变更都伴随全量重渲染，数据量大时存在性能压力。
4. **「三视图两实现」陷阱**：`showView('schedule')`（L11711）实际激活的是 `#viewAudit` 而非 DOM 中真实存在的 `#viewSchedule`（后者成为孤儿节点，永不进入 active），排查视图问题易误导。
5. **离线版对 CDN 强依赖**：`downloadBoardAsHTML` 内联了 JS/CSS，但 `<head>` 仍保留 Tailwind/Chart.js/xlsx 的 CDN `<script>`，离线版需联网才能完整渲染。
6. **`var` 混用与全局变量**：大量 `var` 全局函数/变量（如 `currentDetailEmp`、`_mgrCurrentGroup`），模块边界靠命名约定而非作用域隔离，重构风险高。

---

## 8. XSS 防护确认
- `core/utils.js` 与 `index.html` 内联均定义了 `escHtml` / `escAttr`（L1544 / L1558）。
- `renderTable` 所有用户输入值（员工、组织、日期、明细 tooltip）均经转义；行内 `onclick` 的 key 经 `escAttr` 转义。
- 异常卡片、明细弹窗等动态拼接处同样使用 `escHtml`/`escAttr`。
- 结论：**存在基础 XSS 防护**，但 `openDetail` 等弹窗中仍有部分 `style="..."` 字符串拼接依赖前端数据，若数据源被污染仍需警惕。

---

*本报告所有行号、变量名、函数名均来自对真实源码的逐行核对；如需进一步细化某一函数，可基于上述行号直接定位。*
