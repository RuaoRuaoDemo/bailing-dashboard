# 百灵仪表盘（Bailing Dashboard）交互与耦合分析

> 分析对象：`C:\Users\RuaoWork\Desktop\fuke-bailing-dashboard\index.html`
> 分析基准：**真实源码逐行核对**
> 配套文档：《百灵仪表盘_代码分析报告.md》

---

## 0. 修订说明

旧版交互分析的行号、变量名与视图路由描述与真实源码不符，本版已据实重写：
- 白名单变量 `SCHEDULE_EMP_WHITELIST` → **`App.whitelistSet`**。
- `calcAll`@2384 → **@2551**；`openDetail`@6739 → **@6914**；`showView`@10883 → **@11690**；`applyFilters`@未给出 → **@3682**；`renderTable`@未给出 → **@3830**；`renderAll`@未给出 → **@11725**。
- 确认 `MutationObserver` 真实存在于 `mountRangePickers` 内（L11981）。
- 确认 `showView('schedule')` 实际激活 `#viewAudit`（三视图两实现陷阱）。

---

## 1. 架构模式总览

系统采用 **「全局状态单例 + 命令式整片重绘」**：

```
用户操作 / 事件
      │
      ▼
 修改 App 状态（原始行 / 选择 / 阈值）
      │
      ▼
  calcAll()  ──►  重聚合 App.dailyData（含白名单裁剪）
      │
      ▼
  applyFilters() ──► 生成 App.filteredData（含注入虚拟行）
      │
      ├─► renderTable()      （表格 + 分页 + 事件委托）
      ├─► renderSummary()    （指标卡）
      ├─► renderTrend()      （Chart.js 趋势图）
      └─► checkAlerts()      （异常检测）
```

特征：**状态与视图单向流动**；视图无独立状态缓存，永远从 `App` 重新派生。优点是逻辑简单、无状态同步 bug；代价是任何微小变更都触发全量重渲染。

---

## 2. 三大重绘枢纽与调用关系

| 枢纽 | 行号 | 触发方 | 它做什么 | 它调用渲染 |
|------|------|--------|---------|-----------|
| `calcAll()` | L2551 | 初始化、上传、清除、手动修正 | 重建映射 → 合并 → **白名单裁剪** → 排序 → 预计算利用率 | 否（只改状态） |
| `renderAll()` | L11725 | 初始化、上传、清除 | 重建筛选项 → 设日期范围 → 回填控件 | `renderTable/Summary/Trend` + `checkAlerts` + `updateFileCards` + `renderAuditView` |
| `applyFilters()` | L3682 | 任何过滤/排序/分页/阈值变更 | 构建 `filteredData`（含分组排除 + 虚拟行注入） | `renderTable/Summary/Trend` + `checkAlerts`（末尾焊死，L3814） |

调用链：
- 初始化：`calcAll()` → `renderAll()` →（内部）各 `render*`。
- 用户改筛选项：`applyFilters()` → 各 `render*`（**不经过 calcAll**，性能友好但依赖 `dailyData` 已是最新）。
- 上传/清除：`calcAll()` → `renderAll()`（因为原始数据变了，必须重聚合）。

⚠️ **关键耦合**：`applyFilters` 直接消费 `App.dailyData`，若某路径在**未调用 `calcAll`** 的情况下改动了 `dailyData`（如手动修正），`applyFilters` 仍能渲染，但 `renderAll` 重建的筛选项可能不包含新数据——潜在不一致。

---

## 3. 事件绑定地图

下表列出 DOM 事件 → 处理函数 → 状态变更 → 重绘的端到端链路。

| DOM 元素 / 事件 | 绑定位置 | 处理函数 | 状态变更 | 触发重绘 |
|----------------|---------|---------|---------|---------|
| `#uploadFilesInput` change | `bindUploadEvents` L10672 | 匿名 onchange | 清空全部状态 → `processAutoFile` 载入 → `calcAll` | `calcAll; renderAll; saveToLocalStorage; autoSetBoardDateRange` |
| 上传引导区拖拽 / 文件夹 | `_initUploadLanding` L10784 | `_handleLandingUpload` | 同上传 | 同上传 |
| `#clearAllBtn` click | L10722 | `clearAll` | 清空全部状态 | `calcAll; renderAll; localStorage.remove` |
| `#thresholdLow/High` change | L10724 | 匿名 | `applyFilters` | `applyFilters`（焊死重绘） |
| `#filterGroup` change | L10726 | 匿名 | `autoSetFilterDateRange; applyFilters` | 同上 |
| `#sortField/#sortDirection` change | L10730 | `(e)=>` | `App.sortField/Direction; currentPage=1` | `renderTable` |
| `#pageSizeSelect` change | L10732 | `(e)=>` | `App.pageSize; currentPage=1` | `renderTable` |
| `#jumpPageBtn` click | L10733 | `()=>` | `App.currentPage` | `renderTable` |
| `.nav-btn` click | L11833 | `showView(v)` | 切换 active 视图 | `showView` 内选择性渲染 |
| 表格行 `tr.onclick` | `renderTable` 委托 | `openDetail` | `currentDetailEmp/Date; _detailSource` | `showView('detail')` → `openDetail` 内重算渲染 |
| 行内按钮 `onclick` | `renderTable` L3872 | `toggleIgnoreRow` / `editDailyRowByKey` | `ignoredKeys` / 手动行 | 通常 `renderAll` 或 `applyFilters` |
| 异常卡片「跳转主看板」 | `renderAnomalyView` L8866 | `switchToDashboardAndFilter` | 设员工/日期筛选 | `showView('dashboard'); applyFilters` |
| 异常卡片「展开详情」 | L8863 | `toggleAlertDetail` | 切换 `hidden` class | 仅 DOM，无重绘 |
| `#mgrMatrixContent` 单元格 click | `renderManageBoard` L11525 | `showMgrStdDetail` | 弹窗内容 | `_showMgrModal`（独立 Modal） |
| 管理看板组切换 | `switchMgrGroup` L11367 | `renderManageBoard` | `_mgrCurrentGroup` + 日期 | `renderManageBoard` |
| 审核子 Tab | `switchAuditSubTab` L11146 | `renderPersonSummary` / `renderScheduleView` | Tab active 状态 | 对应子视图渲染 |
| 日期范围对 change | `mountRangePickers` L11949 | `BailingRangePicker.attach` 的 onChange | 日期值 | 各异：主看板 null / 审核 `renderAuditView+renderTable` / 班表 `renderScheduleMatrix` / 筛选 `applyFilters` / 管理 `renderManageBoard` |
| hashchange | `handleHashRouting` L11931 | `showView(hash)` | 同 nav-btn | 同 `showView` |

---

## 4. 视图路由：`showView()`（L11690）

`showView(name)` 先清除所有 `.view-section` 的 `active`，再按 `name` 激活对应视图：

| name | 激活的 DOM | 附加动作 |
|------|-----------|---------|
| `dashboard` | `#viewDashboard` | `setTimeout(renderTrend, 50)` |
| `detail` | `#viewDetail` | — |
| `audit` | `#viewAudit` | 重置审核日期 → `renderAuditView()` + `renderTable()` |
| `schedule` | **`#viewAudit`**（非 `#viewSchedule`） | `switchAuditSubTab('schedule')` |
| `manage` | `#viewManage` | `renderManageBoard()` |

### ⚠️ 「三视图两实现」陷阱
- DOM 中真实存在 5 个视图节点：`#viewDashboard / #viewManage / #viewDetail / #viewAudit / #viewSchedule`（均见于 L807–1285）。
- 但 `showView('schedule')` **绕过 `#viewSchedule`，改去激活 `#viewAudit`** 并切换到其内部的 schedule 子 Tab（L11711–11716）。
- 因此 `#viewSchedule` 是一个**孤儿节点**：永远不进入 `active`，其中的静态内容不会显示。
- 排查「班表为什么在 audit 里」或「schedule 视图空白」时，务必注意此路由重定向，否则会被 `#viewSchedule` 的存在误导。

---

## 5. 交互函数清单（含真实行号）

| 函数 | 行号 | 角色 |
|------|------|------|
| `bindUploadEvents` | L10668 | 文件上传、清除、阈值/排序/分页/跳转绑定 |
| `updateFileCards` | L10736 | 根据数据有无切换上传引导页 / 看板 / 工具栏显隐 |
| `_initUploadLanding` | L10784 | 拖拽 + 文件夹选择上传 |
| `_handleLandingUpload` | L10868 | 引导页上传主流程 |
| `downloadDataTemplate` | L10916 | 下载 4 个示例 CSV + 说明的 ZIP |
| `downloadBoardAsHTML` | L10999 | 打包离线版 HTML（内联 JS/CSS + 数据快照） |
| `switchAuditSubTab` | L11146 | 审核视图子 Tab（daily/person/schedule）切换 |
| `renderPersonSummary` | L11175 | 人维度周期汇总表（含排序箭头） |
| `renderManageBoard` | L11378 | Google Sheet 风格矩阵 |
| `showMgrStdDetail` | L11542 | 管理看板单元格明细弹窗 |
| `showView` | L11690 | 视图路由中枢 |
| `renderAll` | L11725 | 全量刷新枢纽 |
| `saveToLocalStorage` / `loadFromLocalStorage` | L11761 / — | 持久化 |
| `mountRangePickers` | L11947 | 挂载所有日期范围对 + `MutationObserver` |
| `autoSetFilterDateRange` | L8555 | 按分组/员工智能定主看板周期 |
| `autoSetScheduleDateRange` | L8589 | 班表矩阵日期联动 |
| `renderAnomalyView` | L8639 | 异常分析视图 |
| `switchToDashboardAndFilter` | L8922 | 从异常卡片跳回主看板并筛选 |
| `navigateToRow` | L8973 | 跳转指定行（员工+日期） |
| `renderAuditView` | L9003 | 审核视图渲染 |
| `openDetail` | L6914 | 员工×日期明细（独立重算） |
| `applyFilters` | L3682 | 当前视图过滤枢纽 |
| `renderTable` | L3830 | 主表渲染 |
| `renderSummary` | L4559 | 指标卡 |
| `renderTrend` | L4683 | 趋势图 |
| `checkAlerts` | L6583 | 异常检测 |
| `calcAll` | L2551 | 聚合枢纽 |

---

## 6. 三大耦合点与隐患

### 6.1 `calcAll` ↔ 白名单破坏性裁剪（强耦合 + 数据损失）
`calcAll`（L2673–2741）对 `dailyData/utilRows/rawRows/nontaskRows` **原地 `.filter()`**，白名单与 org 不匹配的行被永久删除。
- 后果：后续任何功能（包括「查看原始数据」「导出」）都只能看到白名单内的数据；若白名单配置错误，数据**不可恢复**。
- 耦合：白名单来自 `App.scheduleWhitelist`（排班表），因此**排班表缺失某人 = 该人所有数据消失**，且无告警。

### 6.2 `openDetail` 独立重算 ↔ `calcAll` 主表口径漂移（逻辑耦合）
- 主表 `dailyData` 在 `calcAll` 阶段已把非任务合并进日维度（`nts = minutes/60*125`）。
- `openDetail`（L6940–6952）却**另行**从 `App.rawRows`/`App.nontaskRows` 重跑 `calcRow`，并用正则 `/培训|考试|开会|值班|还休|其他审核任务|团建|会议/` 防双算。
- 风险：两套口径对「培训/开会」等边界任务的判断若不一致（如正则未覆盖新任务名、或非任务行未含同名 task），明细页合计 vs 主表合计会出现偏差，且难以定位。

### 6.3 计算与渲染焊死（调用耦合 + 性能）
`applyFilters`（L3814）与 `renderAll`（L11756）末尾都强制整片重绘。
- 任何过滤/排序/分页/阈值调整都触发 `renderTable+renderSummary+renderTrend+checkAlerts`。
- `renderTrend`/`checkAlerts` 涉及 Chart.js 实例销毁重建（`safeDestroyChart`，L9175），高频操作有性能开销。
- 建议（非阻塞）：对纯排序/分页变更，可仅 `renderTable` 而不重算趋势与告警。

---

## 7. XSS 防护确认（交互安全）

- 所有用户可控字段在插入 `innerHTML` 前经 `escHtml` / `escAttr`（L1544 / L1558）转义。
- 表格行内的 `onclick` key 使用 `escAttr(escKey)` 包裹，`editDailyRowByKey`/`toggleIgnoreRow` 接收转义后的 key。
- 异常卡片、`openDetail`、管理看板弹窗等动态拼接处均使用转义函数。
- 风险残余：部分 `style="..."` 与 `data-tip="..."` 仍由前端数据拼接（如 `buildCellDetail`），在数据源被污染（如上传恶意 CSV）时仍可能注入；上传链路建议在解析层做字段白名单校验。

---

## 8. 交互流程示例

### 8.1 上传数据全流程
```
选择文件 → uploadInput.onchange (L10672)
  → localStorage.removeItem 清缓存
  → 清空 App 全部状态（rawRows/nontaskRows/utilRows/dailyData/...）
  → processAutoFile 逐文件解析（try/catch 单文件容错）
  → updateFileCards() 切到看板视图
  → calcAll() 重聚合 + 白名单裁剪
  → renderAll() 全量渲染
  → saveToLocalStorage() 持久化
  → autoSetBoardDateRange(true) 重算日期范围
  → syncGoogleSchedule() 自动同步排班
```

### 8.2 点击某员工某天查看明细
```
renderTable 行 tr.onclick（委托，排除按钮/input）
  → openDetail(emp, date)
    → 记录 _detailSource（audit/dashboard/alert）
    → 从 App.rawRows 现查该行
    → 独立重算当日汇总（calcRow + 防双算正则）
    → 查 utilRows 取利用率
    → showView('detail')
    → 渲染 KPI / 历史均值 / 7日趋势
    → detailDoughnut.destroy() 后重建圆盘图
```

### 8.3 异常卡片跳回主看板
```
renderAnomalyView 按钮 onclick="switchToDashboardAndFilter(emp,date)"
  → showView('dashboard')
  → 勾选 filterNameMulti 中该员工 checkbox
  → 设 filterDateFrom/To = 该日期
  → applyFilters()（焊死重绘）
  → scrollIntoView 表格
```

---

*本分析所有行号、函数名、事件链路均来自对真实源码的逐行核对，可与《百灵仪表盘_代码分析报告.md》对照使用。*
