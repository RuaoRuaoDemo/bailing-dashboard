# 百灵工时数据仪表盘 - 项目复刻提示词

> 使用本提示词，AI 可快速复刻本项目或适配类似场景（如其他团队的工时管理系统）

---

## 一、项目概述

**项目名称**：百灵工时数据仪表盘（bailing-dashboard）

**项目类型**：单文件 Web 应用（纯前端，零后端）

**核心功能**：
1. 多表数据融合（工作量表 + 非任务表 + 利用率底表）
2. 标准条自动换算（通航/非通航/计时/非任务）
3. 工时利用率计算与异常检测
4. 排班管理（矩阵式班表）
5. 多维度数据可视化（类目维度/人员维度）
6. 个人画像（周期趋势/明细弹窗）
7. 数据持久化（localStorage）

**适用场景**：
- 标注团队工时管理
- 呼叫中心绩效考核
- 项目制团队产出统计
- 任何需要"标准条"换算的绩效场景

---

## 二、技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| HTML5 | - | 页面结构 |
| CSS3 (Tailwind via CDN) | 3.x | 样式布局 |
| JavaScript (Vanilla) | ES6+ | 核心逻辑 |
| Chart.js | 4.x | 图表绘制 |
| SheetJS (xlsx) | 0.20.x | Excel 文件解析 |
| Day.js | 1.x | 日期处理 |
| localStorage | - | 数据持久化 |

**外部 CDN 引用**（在 HTML `<head>` 中）：
```html
<!-- Tailwind CSS -->
<script src="https://cdn.tailwindcss.com"></script>
<!-- Chart.js -->
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.7/dist/chart.umd.min.js"></script>
<!-- SheetJS -->
<script src="https://cdn.sheetjs.com/xlsx-0.20.3/package/dist/xlsx.full.min.js"></script>
<!-- Day.js -->
<script src="https://cdn.jsdelivr.net/npm/dayjs@1.11.13/dayjs.min.js"></script>
```

---

## 三、文件结构

```
bailing-dashboard.html          # 主文件（单文件架构）
├─ <style>                      # 样式区
│   ├─ Tailwind 配置             # 自定义颜色/字体
│   ├─ 响应式媒体查询             # 移动端适配
│   └─ 自定义组件样式             # 按钮/卡片/弹窗等
├─ <div id="app">              # 主容器
│   ├─ 横幅区                    # 项目名 + 天气
│   ├─ 异常告警栏                 # 顶部异常提示
│   ├─ 导航栏                    # 主看板 | 数据&班次详情
│   ├─ 主看板视图                 # 统计卡片 + 四看板
│   ├─ 数据详情视图               # 日维度 | 人维度 | 班表
│   ├─ 个人详情弹窗               # 单日/周期详情
│   └─ 各类 Modal               # 导入/编辑/筛选弹窗
├─ <script>                     # 逻辑区
│   ├─ 核心公式库                # BailingFormulas
│   ├─ 状态管理                  # BailingStore
│   ├─ 数据持久化                # BailingPersistence
│   ├─ 工具函数                  # BailingUtils
│   ├─ 图表渲染                  # renderChart 系列
│   ├─ 事件处理                  # 导入/点击/筛选
│   └─ 初始化                   # DOMContentLoaded
```

> **关键设计**：单文件架构，所有代码在一个 HTML 文件中，双击即用。

---

## 四、核心业务规则（复刻时必须理解）

### 4.1 标准条换算公式

**为什么需要"标准条"？**
不同任务类型的工作量无法直接比较（审核量 vs 时长），需要统一换算成"标准条"作为绩效基准。

| 任务类型 | 换算公式 | 说明 |
|---------|---------|------|
| **通航审核** | `审核量 ÷ 管理效能 × 1000` | 管理效能 = 审核量对应1000标准条的基准值 |
| **非通航审核** | `审核量 ÷ 管理效能 × 1000` | 同上 |
| **计时任务** | `时长(分钟) ÷ 60 × 125` | 1小时 = 125标准条 |
| **非任务活动** | `时长(分钟) ÷ 60 × 125` | 同计时任务（培训/会议/考试等） |

**日理论产出**：
- 全天班(8h) = 1000 标准条
- 半天班(4h) = 500 标准条

**关键代码逻辑**（JavaScript）：
```javascript
// 标准条换算核心函数
function calcRow(row, empName, dateStr) {
  // 1. 通航审核标准条
  const hangxiaoStd = row.hangxiaoQty / row.efficiency * 1000;
  
  // 2. 计时任务标准条
  const timerStd = row.timerMinutes / 60 * 125;
  
  // 3. 非任务标准条（从非任务表合并）
  const nontaskStd = row.nontaskMinutes / 60 * 125;
  
  return {
    hangxiaoStd,
    timerStd,
    nontaskStd,
    totalStd: hangxiaoStd + feihangxiaoStd + timerStd + nontaskStd
  };
}
```

---

### 4.2 工时利用率公式

**公式**：
```
利用率 = ((计量h + 计时h) × 0.8125 + 通航审核h) ÷ 出勤h × 100%
```

**参数说明**：
- `计量h`：非通航类计量任务小时数
- `计时h`：计时任务小时数
- `0.8125`：换算系数（为什么是这个值？因为 1000÷8÷125 = 0.8125）
- `通航审核h`：通航类审核小时数（有11h上限修正）
- `出勤h`：实际出勤时长

**异常阈值**：
- 🟢 正常：81.25% ~ 130%
- 🟡 轻微异常：<81.25% 或 >130%，偏离<2.5%
- 🟠 一般异常：偏离 2.5%~5%
- 🔴 严重异常：偏离 >5%

**关键代码逻辑**：
```javascript
function calcUtilRate(row) {
  const effectiveHours = 
    (row.jiliangH + row.jishiH) * 0.8125 + row.hangxiaoH;
  const rate = effectiveHours / row.attendanceH * 100;
  
  // 异常检测
  let level = 'normal';
  if (rate < 81.25) {
    const deviation = (81.25 - rate) / 81.25;
    if (deviation > 0.05) level = 'severe';
    else if (deviation > 0.025) level = 'moderate';
    else level = 'mild';
  }
  
  return { rate, level };
}
```

---

### 4.3 数据表合并逻辑

**问题**：三张表（工作量表/非任务表/利用率底表）如何关联？

**解决方案**：以 **员工姓名 + 日期** 作为联合主键，进行 LEFT JOIN 式合并。

**合并步骤**：
```
1. 以工作量表为主表（最全的员工+日期组合）
2. 遍历非任务表，按"员工+日期"匹配，合并非任务时长
3. 遍历利用率底表，按"员工+日期"匹配，合并出勤时长
4. 如匹配失败，留空（不报错）
```

**关键代码逻辑**：
```javascript
// 构建联合主键索引
const index = {};
dailyData.forEach(row => {
  const key = `${row.empName}|${row.date}`;
  index[key] = row;
});

// 合并非任务数据
nontaskData.forEach(ntRow => {
  const key = `${ntRow.empName}|${ntRow.date}`;
  if (index[key]) {
    index[key].nontaskMinutes = ntRow.totalMinutes;
  }
});
```

**常见坑点**（复刻时注意）：
- ⚠️ 员工姓名可能有别名（如"张三" vs "张三(Zhang San)"）
- ⚠️ 日期格式可能不统一（2026-05-01 vs 2026/5/1）
- ⚠️ 非任务表的绩效字段单位是**分钟**，需要÷60转小时

---

## 五、UI 设计模式（可直接复用）

### 5.1 统计卡片设计

**样式特征**：
- 白色背景 + 轻微阴影
- 顶部彩色边框（左边缘 4px）
- 大字号显示数值 + 小字号显示标签
- 悬停时轻微放大（transform: scale(1.02)）

**代码模板**：
```html
<div class="stat-card bg-white rounded-lg shadow p-4 border-l-4 border-blue-500">
  <div class="text-sm text-gray-500">总人数</div>
  <div class="text-2xl font-bold text-gray-800">42</div>
  <div class="text-xs text-green-500 mt-1">↑ 12% vs 上周</div>
</div>
```

---

### 5.2 图表联动设计

**交互逻辑**：
1. 用户在顶部选择日期范围
2. 所有图表自动刷新（通过自定义事件 `dateRangeChanged`）
3. 点击图表中的数据项，跳转到详细视图

**代码模板**：
```javascript
// 日期筛选变化时，触发所有图表刷新
document.getElementById('dateFrom').addEventListener('change', () => {
  renderAllCharts();
  renderDataTable();
});

function renderAllCharts() {
  renderCategoryChart();    // 类目维度
  renderPersonChart();      // 人员维度
  renderTrendChart();      // 趋势图
}
```

---

### 5.3 弹窗设计模式

**三种弹窗类型**：
1. **Modal**（居中弹窗）：用于导入、编辑、详情展示
2. **侧边栏**（右侧滑出）：用于个人详情（宽度 60%）
3. **Tooltip**（悬停提示）：用于图表数据展示

**Modal 代码模板**：
```html
<div id="myModal" class="modal-overlay hidden">
  <div class="modal-content bg-white rounded-lg shadow-xl max-w-2xl">
    <div class="modal-header flex justify-between items-center p-4 border-b">
      <h3 class="text-lg font-semibold">标题</h3>
      <button onclick="closeModal('myModal')">✕</button>
    </div>
    <div class="modal-body p-4">
      <!-- 内容 -->
    </div>
    <div class="modal-footer p-4 border-t text-right">
      <button class="btn-secondary">取消</button>
      <button class="btn-primary">确认</button>
    </div>
  </div>
</div>
```

**打开/关闭逻辑**：
```javascript
function openModal(id) {
  document.getElementById(id).classList.remove('hidden');
}

function closeModal(id) {
  document.getElementById(id).classList.add('hidden');
}

// 点击外部关闭
document.getElementById('myModal').addEventListener('click', (e) => {
  if (e.target.classList.contains('modal-overlay')) closeModal('myModal');
});
```

---

### 5.4 表格交互设计

**功能清单**：
- ✅ 表头点击排序（升序/降序切换）
- ✅ 行点击展开详情
- ✅ 单元格点击弹窗明细
- ✅ 顶部筛选栏（日期/员工/分组）
- ✅ 分页或虚拟滚动（大数据量）

**表格代码模板**：
```html
<table class="data-table w-full text-sm">
  <thead>
    <tr>
      <th class="sortable" onclick="sortTable('name')">员工 ↕</th>
      <th class="sortable" onclick="sortTable('std')">标准条 ↕</th>
    </tr>
  </thead>
  <tbody>
    <tr onclick="showDetail(this)" class="hover:bg-gray-50 cursor-pointer">
      <td>张三</td>
      <td>1050</td>
    </tr>
  </tbody>
</table>
```

---

## 六、数据导入流程（关键步骤）

### 6.1 Excel 文件解析

**使用 SheetJS 库**：
```javascript
function importExcel(file, callback) {
  const reader = new FileReader();
  reader.onload = (e) => {
    const workbook = XLSX.read(e.target.result, { type: 'array' });
    const sheet = workbook.Sheets[workbook.SheetNames[0]];
    const data = XLSX.utils.sheet_to_json(sheet, { header: 1 });
    callback(data);
  };
  reader.readAsArrayBuffer(file);
}
```

### 6.2 列名模糊匹配

**问题**：用户上传的 Excel 列名可能不标准（如"员工姓名" vs "姓名" vs "Name"）

**解决方案**：预设别名映射表
```javascript
const COLUMN_ALIASES = {
  '员工姓名': ['姓名', '名字', 'Name', '员工'],
  '日期': ['日期', 'Date', '数据日期'],
  '通航审核量': ['通航', '通航审核', 'Hangxiao'],
  // ...
};

function matchColumn(header, aliases) {
  for (const alias of aliases) {
    if (header.includes(alias)) return true;
  }
  return false;
}
```

### 6.3 数据验证

**导入后自动检查**：
- 必填列是否完整
- 日期格式是否正确
- 数值是否为负数
- 员工姓名是否在已知列表中

---

## 七、本地持久化方案

### 7.1 存储结构

```javascript
// 存储排班数据
localStorage.setItem('bailing_schedule', JSON.stringify(scheduleData));

// 存储导入的原始数据
localStorage.setItem('bailing_data_workload', JSON.stringify(workloadData));
localStorage.setItem('bailing_data_nontask', JSON.stringify(nontaskData));
localStorage.setItem('bailing_data_utilization', JSON.stringify(utilData));

// 存储用户设置
localStorage.setItem('bailing_settings', JSON.stringify({
  dateRange: { from: '2026-05-01', to: '2026-05-31' },
  thresholds: { min: 81.25, max: 130 }
}));
```

### 7.2 自动保存

**在以下时机触发保存**：
- 数据导入完成后
- 排班修改后
- 用户设置变更后

**恢复数据**：
```javascript
window.addEventListener('DOMContentLoaded', () => {
  const savedData = localStorage.getItem('bailing_data_workload');
  if (savedData) {
    workloadData = JSON.parse(savedData);
    renderDashboard();
  }
});
```

---

## 八、适配新项目的修改清单

如果你要复刻本项目用于其他团队，需要修改以下内容：

### 8.1 必须修改的项

| 修改项 | 位置 | 说明 |
|--------|------|------|
| **项目名称** | HTML `<title>` 和横幅区 | 替换为你的项目名称 |
| **组织过滤规则** | `filterByOrganization()` 函数 | 改为你的组织架构 |
| **标准条公式** | `BailingFormulas.calcRow()` | 如不需要标准条，改为直接用原数值 |
| **数据表列名** | `COLUMN_ALIASES` 对象 | 匹配你的 Excel 列名 |
| **异常阈值** | `detectAnomalies()` 函数 | 根据你的业务规则调整 |

### 8.2 可选修改的项

| 修改项 | 说明 |
|--------|------|
| 颜色主题 | Tailwind 配置中的 `primaryColor` |
| 图表类型 | Chart.js 配置（柱状图/折线图/饼图） |
| 统计卡片数量 | 可增删卡片 |
| 班次类型 | 如不需要排班，可隐藏班表功能 |

### 8.3 不需要的功能（可删除）

- 排班管理（如团队不考核出勤）
- 非任务表（如团队无非任务活动）
- 异常检测（如不需要自动告警）

---

## 九、常见问题与解决方案

### Q1：导入大文件（50万行）时页面卡死？

**原因**：JavaScript 单线程，同步解析阻塞 UI

**解决方案**：
1. 使用 Web Worker 异步解析
2. 或分块读取（每次 1 万行）
3. 或限制文件大小（提示用户拆分文件）

### Q2：localStorage 容量不够？

**原因**：localStorage 上限 5-10MB

**解决方案**：
1. 压缩存储（使用 LZ-string 库）
2. 或改为 IndexedDB（支持几百MB）
3. 或每次重新导入（不持久化）

### Q3：图表显示不正确？

**检查清单**：
- ✅ Chart.js 版本是否正确（推荐 4.x）
- ✅ 数据格式是否为数组（不能是对象）
- ✅ 颜色值是否合法（不能用 `undefined`）
- ✅  canvas 容器是否有宽度（不能是 0）

### Q4：如何部署到服务器？

**方案 1**：直接上传 HTML 文件到静态服务器（推荐）
```
https://your-domain.com/bailing-dashboard.html
```

**方案 2**：内网部署（如 Nginx）
```
server {
  location /bailing {
    root /var/www/html;
    index bailing-dashboard.html;
  }
}
```

**注意**：如部署到服务器，建议添加登录验证（否则数据会互相覆盖）

---

## 十、完整提示词模板（直接复制给 AI）

如果你要让 AI 帮你复刻或修改本项目，直接复制以下提示词：

```
---
角色：高级前端工程师 + 数据分析师

任务：复刻"百灵工时数据仪表盘"项目，用于 [你的团队名称] 的工时管理

项目要求：
1. 单文件 HTML 应用（所有代码在一个文件中）
2. 支持导入 Excel 数据（工作量表/非任务表/利用率底表）
3. 自动计算 [你的换算公式，可参考标准条公式]
4. 可视化展示 [你需要的图表类型]
5. 支持 [你的特殊需求，如排班管理/异常检测等]

技术栈：
- HTML5 + CSS3 (Tailwind CSS via CDN)
- Vanilla JavaScript (ES6+)
- Chart.js (图表)
- SheetJS (Excel 解析)

数据表结构：
- 工作量表：[列出你的列名]
- 非任务表：[列出你的列名]
- 利用率底表：[列出你的列名]

业务规则：
- 标准条公式：[你的公式]
- 异常阈值：[你的阈值]
- 组织过滤：[你的组织架构]

参考资料：
[附上百灵仪表盘使用说明.md 的核心章节]
---
```

---

## 十一、项目文件清单

| 文件 | 说明 | 是否必须 |
|------|------|---------|
| `bailing-dashboard.html` | 主程序文件 | ✅ 必须 |
| `百灵仪表盘使用说明.md` | 使用文档 | ⭕ 推荐 |
| `数据格式模板.xlsx` | 标准数据格式 | ⭕ 推荐 |
| `SOP_导入流程.pdf` | 打印版SOP | ❌ 可选 |

---

## 十二、更新日志

| 版本 | 日期 | 修改内容 |
|------|------|---------|
| V1.0 | 2026-05-15 | 初始版本 |
| V8.96 | 2026-05-15 | 完善功能，发布SOP |

---

**文档结束**

---

*本提示词文档为百灵工时数据仪表盘项目的配套文档，用于项目复刻和二次开发。*

*最后更新：2026-06-18*
